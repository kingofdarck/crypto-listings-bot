# Подробное описание поиска листингов

## Общий принцип работы

Бот работает в режиме 24/7 и каждые 30 секунд проверяет официальные API бирж на предмет новых анонсов о предстоящих листингах. Основная задача - найти анонсированные листинги с точным временем запуска торговли.

## Архитектура системы

### 1. Основной цикл (main.py)
```
Запуск → Загрузка данных → Цикл проверки (каждые 30 сек) → Сохранение данных
```

**Что происходит в цикле:**
- Вызывается `check_listings()` для получения новых листингов
- Проверяются предстоящие листинги на близость к времени запуска
- Отправляются уведомления (каждые 5 минут + алерты)
- Данные сохраняются в JSON файл

### 2. Мониторинг бирж (exchange_monitor.py)

Для каждой биржи используется свой метод поиска:

## Детальный разбор по биржам

### 🟡 BINANCE
**API Endpoint:** `https://www.binance.com/bapi/composite/v1/public/cms/article/list/query`

**Алгоритм поиска:**
1. **Запрос анонсов:** POST запрос с параметрами:
   ```json
   {
     "type": 1,
     "catalogId": 48,  // Категория листингов
     "pageNo": 1,
     "pageSize": 20
   }
   ```

2. **Фильтрация заголовков:** Ищем ключевые слова в заголовках статей:
   - `"will list"` - "Binance will list TOKEN"
   - `"listing"` - "TOKEN listing announcement"  
   - `"launches"` - "Binance launches TOKEN"

3. **Извлечение символа токена:** Регулярное выражение `\b([A-Z]{2,10})\b`
   - Находит последовательности из 2-10 заглавных букв
   - Пример: из "Binance Will List Pepe (PEPE)" извлекает "PEPE"

4. **Парсинг времени листинга:** Анализ содержимого статьи на предмет:
   - Точных дат: "2024-01-15 14:00 UTC"
   - Относительного времени: "in 2 hours", "tomorrow at 14:00"
   - Описательного времени: "Trading will start at 14:00 UTC"

**Пример анонса Binance:**
```
Заголовок: "Binance Will List Arbitrum (ARB) with Seed Tag Applied"
Содержание: "...Trading will start on 2024-03-23 at 06:00 (UTC)..."
Результат: TOKEN=ARB, TIME=2024-03-23 06:00 UTC
```

### 🟠 BYBIT  
**API Endpoint:** `https://api.bybit.com/v5/announcements?category=new_crypto&limit=20`

**Алгоритм поиска:**
1. **Запрос анонсов:** GET запрос к официальному API анонсов
2. **Фильтрация по категории:** Только `new_crypto` анонсы
3. **Поиск ключевых слов:**
   - `"listing"` - объявления о листинге
   - `"launch"` - запуск торговли
   - `"will list"` - будущие листинги
   - `"coming soon"` - скоро появится

4. **Извлечение данных:**
   - Символ токена из заголовка
   - Время из `publishTime` или содержимого
   - URL анонса для подробностей

**Пример анонса Bybit:**
```json
{
  "title": "Bybit Spot Listing: Worldcoin (WLD)",
  "content": "Trading starts: July 24, 2024 at 10:00 UTC",
  "publishTime": 1690200000000,
  "url": "https://announcements.bybit.com/..."
}
```

### 🔵 KUCOIN
**API Endpoint:** `https://api.kucoin.com/api/v1/announcements?currentPage=1&pageSize=20`

**Алгоритм поиска:**
1. **Запрос анонсов:** GET запрос к API анонсов
2. **Фильтрация заголовков:** Поиск ключевых слов:
   - `"listing"` - анонсы листингов
   - `"launch"` - запуск торговли  
   - `"will list"` - планируемые листинги
   - `"coming soon"` - скоро

3. **Обработка времени:**
   - Парсинг `publishTime` (timestamp)
   - Извлечение времени из текста анонса
   - Конвертация в UTC

### 🔴 MEXC и OKX
**Статус:** Временно отключены

**Причина:** 
- MEXC: Нет публичного API для анонсов
- OKX: API анонсов требует авторизацию

**Планы:** Добавление парсинга их веб-сайтов или Twitter API

## Алгоритм извлечения времени

### Функция `_extract_listing_time()`

**Поддерживаемые форматы времени:**

1. **Точные даты с временем:**
   ```
   2024-01-15 14:00 UTC
   January 15, 2024 at 14:00 UTC  
   15 Jan 2024, 14:00 UTC
   ```

2. **Время с указанием даты:**
   ```
   14:00 UTC on January 15
   Trading starts at 14:00 UTC
   ```

3. **Относительное время:**
   ```
   in 2 hours
   in 3 days  
   after 1 hour
   ```

**Алгоритм парсинга:**
1. **Регулярные выражения:** Поиск паттернов времени в тексте
2. **dateutil.parser:** Умный парсинг найденных строк
3. **Валидация:** Проверка что время в будущем
4. **Нормализация:** Приведение к UTC

**Пример работы:**
```python
content = "Trading will start on January 15, 2024 at 14:00 UTC"
# Regex находит: "January 15, 2024 at 14:00"
# dateutil парсит: datetime(2024, 1, 15, 14, 0)
# Проверка: время > сейчас? Да → возвращаем
```

## Логика обработки листингов

### 1. Обнаружение новых листингов
```python
for listing in all_listings:
    if listing.status == 'upcoming' and listing.listing_time > now():
        if not exists_in_database(listing):
            add_to_upcoming_listings(listing)
            log_new_discovery(listing)
```

### 2. Отслеживание времени
```python
for listing in upcoming_listings:
    time_left = listing.listing_time - now()
    
    # Проверяем алерты за: 1 час, 30 мин, 15 мин, 5 мин
    if time_left in [3600, 1800, 900, 300]:  # секунды
        send_alert(listing, time_left)
    
    # Если время пришло
    if time_left <= 0:
        send_listing_started_alert(listing)  # 3 раза
        remove_from_upcoming(listing)
```

### 3. Дедупликация
**Проблема:** Один листинг может появиться в нескольких анонсах

**Решение:** Уникальный ключ `exchange:symbol`
```python
listing_key = f"{listing.exchange}:{listing.symbol}"
if listing_key not in known_listings:
    process_new_listing(listing)
```

## Система уведомлений

### Типы уведомлений:

1. **Регулярные отчеты (каждые 5 минут):**
   - 5 ближайших предстоящих листингов
   - Время до каждого листинга
   - Ссылки на анонсы

2. **Предупреждения о приближении:**
   - За 1 час: "⏰ СКОРО ЛИСТИНГ! Осталось 1 час"
   - За 30 минут: "⏰ СКОРО ЛИСТИНГ! Осталось 30 минут"  
   - За 15 минут: "⏰ СКОРО ЛИСТИНГ! Осталось 15 минут"
   - За 5 минут: "⏰ СКОРО ЛИСТИНГ! Осталось 5 минут"

3. **Уведомления о начале торговли:**
   - В момент листинга: "🚨 ЛИСТИНГ НАЧАЛСЯ!"
   - Дублируется 3 раза каждую минуту
   - Содержит прямые ссылки на торговлю

## Обработка ошибок

### Сетевые ошибки:
- **Таймауты API:** Повтор через 60 секунд
- **HTTP ошибки:** Логирование + продолжение работы
- **Недоступность биржи:** Пропуск этой биржи в текущем цикле

### Ошибки парсинга:
- **Неизвестный формат времени:** Пропуск этого анонса
- **Некорректный символ токена:** Логирование + пропуск
- **Поврежденные данные:** Обработка исключений

### Восстановление:
- **Автоматический рестарт:** При критических ошибках
- **Сохранение состояния:** Данные в JSON файле
- **Загрузка при старте:** Восстановление предыдущих листингов

## Производительность

### Оптимизации:
- **Асинхронные запросы:** Параллельная обработка всех бирж
- **Кэширование:** Избежание повторных запросов
- **Лимиты API:** Соблюдение rate limits бирж
- **Минимальные данные:** Хранение только необходимого

### Мониторинг:
- **Логирование всех операций:** Время запросов, найденные листинги
- **Статистика:** Количество обработанных анонсов
- **Алерты об ошибках:** Уведомления о проблемах с API

## Точность данных

### Источники времени:
1. **Официальные анонсы бирж** (приоритет 1)
2. **API timestamps** (приоритет 2)  
3. **Парсинг текста** (приоритет 3)

### Валидация:
- Время должно быть в будущем
- Символ токена должен соответствовать паттерну
- Биржа должна быть в списке отслеживаемых

### Обновления:
- Если найден более точный анонс того же листинга - обновляем время
- Если листинг отменен - удаляем из списка
- Если время изменилось - уведомляем пользователей